console.log("render.js loaded...");

angular.module('hueSquare')

  .factory('render', function($timeout) {
    'use strict';

    var render = {
      // this.gameContainer = angular.element("#game-container");
    }

    return render;
  });
